Docker就像一个虚拟机一样，可以创建软件
docker pull redis
docker rmi redis

docker ps -a  
docker run --name myredis -d redis
docker stop myredis

docker导出镜像,为一个压缩包     docker save -o my.tar zookeeper3.7
docker导入镜像包(压缩包): docker load -i my.tar


